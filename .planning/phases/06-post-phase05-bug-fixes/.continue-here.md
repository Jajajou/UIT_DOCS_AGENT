---
context: phase
phase: 06-post-phase05-bug-fixes
task: 4
total_tasks: 4
status: complete
last_updated: 2026-05-27T13:48:02.826Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints from this phase._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| content truncation mismatch | agent3 sees truncated content, agent4 sees full — validator always fails | advisory | Fixed in d92caf0 — content[:800], top_n=10 |
| retry counter not incremented | operator.add reducer requires returning the field, not just reading | advisory | Fixed in 6f58b82 — always return validation_retry_count: 1 |

<current_state>
Phase06 COMPLETE. All 4 bug fixes applied and committed on branch feat/student-context-and-validation.

279 tests pass. Eval run pending to confirm improvement from phase05+06 combined.

Uncommitted files from prior sessions still exist (see git status) — these are pre-phase06 changes that need review before committing.
</current_state>

<completed_work>

Phase06 — All tasks done:
- Task 1 (6f58b82): agent4 now returns `"validation_retry_count": 1` — infinite loop fixed
- Task 2 (d92caf0): content 300→800 chars, top_n 15→10 in agent3
- Task 3 (ba76cf1): COHORT+AMENDMENT paths now route through enrich_with_temporal_metadata → filter_by_metadata → rerank
- Task 4 (4b5a381): agent3 thinking prompt has specificity directive ("BẮT BUỘC trích dẫn số liệu chính xác") and think budget 150→350 words
- Test fix (4961217): cohort route unit test updated to expect new destination
</completed_work>

<remaining_work>

Phase06 is complete. Next work:
1. Run eval suite to measure improvement
2. (Optional, deferred) Upgrade agent4 model from Qwen3-4B to Qwen3.5-9B
3. (Optional, deferred) Remove dead code: `response_generation_prompt` (unused), entity/rel handling in `_format_reranked_data`
</remaining_work>

<decisions_made>

- COHORT/AMENDMENT routes through enrich (not skip): filter_by_metadata safe because cohort_year=null for AMENDMENT, Qdrant already filtered for COHORT
- top_n=10 + content[:800] gives ~7000 chars context, less fragmented than 15×300
- Agent4 model stays Qwen3-4B for now — defer upgrade until after eval confirms it's the bottleneck
</decisions_made>

<blockers>
- None — phase complete
</blockers>

## Required Reading (in order)
1. `LangGraph/src/agent/graphs/query_graph.py` — current graph topology after phase06 changes
2. `.planning/phases/06-post-phase05-bug-fixes/SUMMARY.md` — what was changed
3. Memory: session_20260527_phase06_fixes.md — full fix details

## Infrastructure State
- Vast.ai ssh2.vast.ai:11674 A100 40GB LIVE
  - port 8000: Vietnamese embeddings
  - port 8001: Vietnamese reranker (AITeamVN/Vietnamese_Reranker)
  - port 8002: Qwen3.5-9B-Instruct (128k context)
- Docker services: LightRAG, PostgreSQL, Qdrant (via docker compose)
- 10 pre-existing test failures (unrelated): lightrag_client header format, mineru Latin lang, reranker HTTP mode

<context>
Session focused on rethink of system after phase05. Found 2 P0 bugs, 1 P1 data flow gap, 2 P2 prompt issues. Wrote PLAN.md and executed all fixes in single phase. System now ready for eval.

Branch feat/student-context-and-validation has uncommitted changes from earlier sessions (scripts, clients, eval, config) — these predate phase06 and need separate review/commit.
</context>

<next_action>
Run eval: `cd LangGraph && source ../.venv/bin/activate && python tests/eval/temporal_evaluation.py`
Check if Vast.ai tunnels still active first: `ssh -p 11674 root@ssh2.vast.ai "nvidia-smi" 2>/dev/null && echo LIVE || echo DEAD`
</next_action>
