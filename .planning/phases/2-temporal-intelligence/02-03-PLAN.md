---
phase: 02-temporal-intelligence
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [LangGraph/src/agent/clients/reranker.py, LangGraph/src/agent/graphs/query_graph.py, LangGraph/src/agent/agents/agent3_response_generation.py]
autonomous: true
requirements: [FR-02, FR-07, FR-09, UR-02]
---

<objective>
Refine retrieval and reranking logic for temporal awareness, enforce cohort-specific routing, and implement confidence transparency in the user response.

Purpose: Optimize accuracy and improve user trust.
Output: Updated reranker.py, query_graph.py, and agent3_response_generation.py.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2-temporal-intelligence/2-RESEARCH.md
@LangGraph/src/agent/clients/reranker.py
@LangGraph/src/agent/graphs/query_graph.py
@LangGraph/src/agent/agents/agent3_response_generation.py
</context>
<tasks>

<task type="auto">
  <name>Task 1: Optimize Reranking Weights (Local Reranker)</name>
  <files>LangGraph/src/agent/clients/reranker.py</files>
  <action>
    Refine the weighting strategy in the local reranker:
    - Implement weights: **0.55 semantic**, **0.20 temporal**, **0.25 cohort** when a cohort is present.
    - **VBHN Boost:** Apply a small boost (+0.1) to documents with `is_vbhn: true` as they contain consolidated, easier-to-read content.
    - Ensure `calculate_temporal_score` returns 0.0 for `is_archived = True`.
    - Implement "Historical Query" override to bypass amendment deprioritization.
    - Confirm reranker runs locally using the configured local model (e.g., via vLLM or local library).
  </action>
  <verify>
    <automated>pytest LangGraph/tests/test_temporal.py -k test_reranking_weights</automated>
  </verify>
  <done>Local reranker scores items with 55/20/25 weights and prioritizes VBHN.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Cohort and Amendment Retrieval</name>
  <files>LangGraph/src/agent/graphs/query_graph.py</files>
  <action>
    Harden the query graph for local execution:
    - Ensure `filter_by_metadata` node drops non-matching cohorts using local PostgreSQL/Qdrant metadata.
    - **Article-level Filtering:** If a document is amended, check if the query specifically targets an `amended_article`. If so, prioritize the *amending* document.
    - Enforce `MAX_CHAIN_DEPTH = 5` for amendment traversal.
  </action>
  <verify>
    <automated>pytest LangGraph/tests/test_temporal.py -k test_cohort_routing</automated>
  </verify>
  <done>Query graph correctly filters by cohort and articles using local metadata stores.</done>
</task>

</tasks>
<task type="auto">
  <name>Task 3: Confidence Transparency in Responses</name>
  <files>LangGraph/src/agent/agents/agent3_response_generation.py</files>
  <action>
    Update the response generation agent to include temporal confidence warnings:
    - If `rerank_confidence < 0.8` OR if the retrieved documents have high "ambiguity" (multiple amendments, overlapping validity), append a transparent note to the response.
    - Example: "Note: This information is based on documents with 75% confidence. You may want to check Decision 108/2024 for more details."
    - Ensure the tone is helpful and professional, following UIT document naming conventions.
  </action>
  <verify>
    <automated>pytest LangGraph/tests/test_temporal.py -k test_confidence_transparency</automated>
  </verify>
  <done>Agent 3 produces responses that clearly state confidence when temporal ambiguity exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Reranker → LLM | Scored context passed to LLM for final response |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Reranker | mitigate | Ensure archived documents are filtered out before being passed to the LLM context, unless the query is explicitly historical. |
| T-02-06 | Repudiation | Response Generation | mitigate | Log all confidence scores and retrieved document IDs for every query to allow for auditing of "incorrect" temporal answers. |
</threat_model>

<verification>
Run the full temporal test suite. Verify that queries for specific student cohorts (e.g., "Khóa 2022") return the correct regulations and that responses include confidence notes when ambiguous.
</verification>

<success_criteria>
1. Cohort-specific queries return documents valid for that cohort's 6-year window.
2. Amendment traversal resolves to the latest document version.
3. Responses include transparency notes for low-confidence temporal results.
</success_criteria>

<output>
After completion, create .planning/phases/2-temporal-intelligence/2-03-SUMMARY.md
</output>
