---
phase: 02-temporal-intelligence
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [LangGraph/src/agent/agents/agent_temporal_extraction.py, LangGraph/src/agent/graphs/indexing_graph.py]
autonomous: true
requirements: [FR-05, FR-06, FR-08]
---

<objective>
Enhance the Vietnamese temporal extraction accuracy using dateparser and underthesea, and implement bidirectional linking for document amendments in the indexing pipeline.

Purpose: Improve metadata quality for temporal RAG.
Output: Updated agent_temporal_extraction.py and indexing_graph.py.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2-temporal-intelligence/2-RESEARCH.md
@LangGraph/src/agent/agents/agent_temporal_extraction.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate dateparser and underthesea (LOCAL ONLY)</name>
  <files>LangGraph/src/agent/agents/agent_temporal_extraction.py</files>
  <action>
    Update TemporalExtractionAgent to utilize local libraries:
    - Use `dateparser.parse(text, languages=['vi'])` to handle complex Vietnamese dates.
    - Use `underthesea.word_tokenize` and `underthesea.text_normalize` to improve text quality before extraction.
    - **VBHN Prioritization:** Add logic to detect "Văn bản hợp nhất" (Consolidated Document) in the content. If detected, set a `is_vbhn: true` flag in metadata.
    - **Article-level Metadata:** Extract specific articles mentioned in amendments (e.g., "Sửa đổi Điều 5") and store as a list of strings in `amended_articles` metadata field.
    - Ensure all date outputs are in YYYY-MM-DD format.
    - Confirm NO external API calls are made for NLP/Date parsing.
  </action>
  <verify>
    <automated>python3 -c "import dateparser; import underthesea; from agent.agents.agent_temporal_extraction import TemporalExtractionAgent; print('Local integration check pass')"</automated>
  </verify>
  <done>Temporal extraction agent uses local dateparser/underthesea and identifies VBHN/article-level amendments.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Bidirectional Amendment Linking</name>
  <files>LangGraph/src/agent/agents/agent_temporal_extraction.py, LangGraph/src/agent/graphs/indexing_graph.py</files>
  <action>
    Implement logic to maintain bidirectional amendment links in the local PostgreSQL DB:
    - In `TemporalExtractionAgent.extract`, ensure `amends_documents` is correctly populated.
    - In the indexing pipeline:
      - When Doc A amends Doc B:
      - Query PostgreSQL to find Doc B by its `document_number`.
      - If Doc B exists, update its `metadata->'amended_by'` list to include Doc A's ID.
      - **VBHN Linking:** If Doc A is a VBHN, find all documents it consolidates (based on its preamble) and link them.
    - Use a recursion limit of 5 to prevent circularity.
  </action>
  <verify>
    <automated>pytest LangGraph/tests/test_temporal.py -k test_amendment_linking</automated>
  </verify>
  <done>Indexing pipeline correctly updates local document metadata for amendment chains and VBHN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Indexing Pipeline → Database | Read/Write access to update existing document metadata |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | Indexing Graph | mitigate | Ensure the database user used for indexing has limited scope, restricted to the `lightrag_doc_status` table. |
| T-02-04 | Information Disclosure | LLM Extraction | mitigate | Sanitize content sent to LLM for extraction to remove PII if necessary (though documents are public university regs). |
</threat_model>

<verification>
Run `LangGraph/tests/test_temporal.py` to verify that a new document correctly triggers an update to an older document's `amended_by` metadata field.
</verification>

<success_criteria>
1. Vietnamese dates like "15 tháng 9 năm 2024" are parsed correctly as "2024-09-15".
2. When Doc A amends Doc B, Doc B's `amended_by` field in PostgreSQL is updated to include Doc A.
</success_criteria>

<output>
After completion, create .planning/phases/2-temporal-intelligence/2-02-SUMMARY.md
</output>
