---
phase: 02-temporal-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, LangGraph/src/agent/services/ping_service.py, LangGraph/tests/test_ping_service.py, LangGraph/tests/test_temporal.py]
autonomous: true
requirements: [FR-10, DR-02]

must_haves:
  truths:
    - "APScheduler runs a daily job to archive expired documents"
    - "Test scaffolds exist for temporal and monitoring logic"
  artifacts:
    - path: "LangGraph/src/agent/services/ping_service.py"
      provides: "Scheduled archival service"
    - path: "LangGraph/tests/test_ping_service.py"
      provides: "Verification for archival logic"
    - path: "LangGraph/tests/test_temporal.py"
      provides: "Integration tests for temporal RAG"
  key_links:
    - from: "LangGraph/src/agent/services/ping_service.py"
      to: "PostgreSQL (lightrag_doc_status)"
      via: "psycopg2"
      pattern: "UPDATE lightrag_doc_status SET is_archived = true"
---

<objective>
Setup environment dependencies, implement the automated monitoring service for document archival, and create the test suite foundation.

Purpose: Automate document lifecycle management and ensure reliability through testing.
Output: ping_service.py, test scaffolds, updated pyproject.toml.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2-temporal-intelligence/2-RESEARCH.md
@LangGraph/src/agent/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment Setup</name>
  <files>pyproject.toml</files>
  <action>
    Install core temporal intelligence dependencies using uv:
    - dateparser (v1.4.0) for Vietnamese date parsing.
    - underthesea (v6.8.5) for Vietnamese NLP/segmentation.
    - apscheduler (v3.11.2) for task scheduling.
    
    Command: `uv add dateparser underthesea apscheduler`
  </action>
  <verify>
    <automated>uv export --format setuppy | grep -E "dateparser|underthesea|apscheduler"</automated>
  </verify>
  <done>Dependencies added to pyproject.toml and installed in venv.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Automated Monitoring Service</name>
  <files>LangGraph/src/agent/services/ping_service.py</files>
  <action>
    Create ping_service.py using APScheduler (AsyncIOScheduler).
    Requirements:
    - Connect to PostgreSQL (lightrag_doc_status table).
    - Implement `archive_expired_documents()` function:
      - Query documents where `valid_until` is in the past and `is_archived = false`.
      - Update `is_archived = true`, `archived_at = now()`, and `archive_reason = 'Expired'`.
    - Schedule this job to run daily at 00:00.
    - Export a `start_scheduler()` function to be called by the main API entry point.
    - Avoid emojis in logs.
  </action>
  <verify>
    <automated>python3 -c "from agent.services.ping_service import archive_expired_documents; print('Import successful')"</automated>
  </verify>
  <done>ping_service.py implemented with daily archival logic.</done>
</task>

<task type="auto">
  <name>Task 3: Create Test Suite Foundation</name>
  <files>LangGraph/tests/test_ping_service.py, LangGraph/tests/test_temporal.py</files>
  <action>
    Create test scaffolds for Wave 0 verification (per RESEARCH):
    - LangGraph/tests/test_ping_service.py:
      - Test `archive_expired_documents` by inserting a dummy expired doc in DB and verifying it gets archived.
    - LangGraph/tests/test_temporal.py:
      - Create test cases for FR-02 (Amendment detection) and FR-09 (Cohort routing).
      - Use mock data for documents with various `valid_from`, `valid_until`, and `cohort_years`.
  </action>
  <verify>
    <automated>pytest LangGraph/tests/test_ping_service.py -v</automated>
  </verify>
  <done>Test files created and basic archival test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service → Database | Administrative update of document status |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ping_service.py | mitigate | Use parameterized SQL queries for archival updates to prevent SQL injection. |
| T-02-02 | Denial of Service | APScheduler | mitigate | Ensure scheduler runs in a separate thread/task and handles DB connection timeouts gracefully. |
</threat_model>

<verification>
Check that `uv` lock reflects new packages. Run the ping service test to ensure it correctly identifies and updates expired records in a test database environment.
</verification>

<success_criteria>
1. APScheduler correctly schedules a daily job.
2. Expired documents in PostgreSQL are successfully marked as archived.
3. pytest suite runs without failures for the new tests.
</success_criteria>

<output>
After completion, create .planning/phases/2-temporal-intelligence/2-01-SUMMARY.md
</output>
