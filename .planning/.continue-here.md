---
context: default
phase: eval-and-url-fix
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-05-10T13:37:22.905Z
---

<current_state>
Session 2026-05-10. Two parallel work streams:
1. DB URL fix: 68/229 DB file_paths dead (30%). scripts/check_db_urls.py generates SQL UPDATEs. Fuzzy matcher fails on CTDT files (space→dash mismatch). Fix is one-liner.
2. Eval overhaul: temporal_evaluation.py has --retrieval-only flag (stops after rerank_data, checks chunk metadata). Not yet tested — needs LLM endpoint.
</current_state>

<completed_work>
- 200-pair eval set v5.0 audited: 60 val/140 test, 50/50/50/50 buckets. CRITICAL: 180/200 pairs have empty expected_doc_ids — must use doc_number string matching.
- All 166 CSV canonical URLs confirmed live (HTTP 200+PDF). HL correct.
- 145/166 CSV URLs exact-match DB; 21 missing = filename aliases.
- 229 DB PDF file_paths HTTP-checked: 161 live, 68 dead.
- --retrieval-only added to temporal_evaluation.py: call_pipeline_retrieval_only(), extract_retrieved_doc_numbers(), interrupt_before=["agent3_generate_response"]. Default pairs → 200-pair set.
- scripts/check_db_urls.py: async HTTP check + SQL UPDATE generator.
</completed_work>

<remaining_work>
- Task 5: Fix CTDT fuzzy matcher — khoa%20N/khoa N → khoa-N
- Task 6: Apply confirmed SQL UPDATEs via docker exec psql
- Task 7: Test --retrieval-only on 5-pair subset (needs GPU)
- Task 8: Run full 200-pair retrieval-only eval
</remaining_work>

<decisions_made>
- Retrieval-only eval preferred for TDCE: no LLM stochasticity, 20x faster, directly tests temporal ranking, solves missing doc_id problem
- Full pipeline kept as default for backward compat
</decisions_made>

<blockers>
- LLM/GPU endpoint down (A100 offline). vast.ai RTX 4090 recommended.
- 68 dead DB URLs break citation links
</blockers>

## Infrastructure State
- DB: live, container uit_docs_agent-postgres_uit-1, creds uitrag/admin123, db lightrag
- /tmp/db_pdf_paths.txt: 229 DB PDF URLs (regenerate if /tmp cleared)
- Canonical CSV: ~/Downloads/pdf_source_urls.csv (166 URLs)
- LangGraph + LLM: DOWN

<next_action>
Fix CTDT fuzzy matcher in scripts/check_db_urls.py normalize_filename():
  add: filename = filename.replace('%20', '-').replace(' ', '-')
Then: source .venv/bin/activate && python scripts/check_db_urls.py
Review UPDATEs, apply via docker exec psql.
</next_action>
