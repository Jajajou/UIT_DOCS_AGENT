---
context: research
phase: null
task: 5
total_tasks: 8
status: in_progress
last_updated: 2026-05-03T02:28:43.859Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Filename truncation — Firecrawl truncates long PDF filenames AND adds 8-char hash suffix. Exact match in `find_urls_by_filename()` fails silently. Must use prefix/fuzzy match.
- [ ] CONSTRAINT: Metadata RAG subgraph is deprecated — Do NOT use `test_metadata_rag_subgraph`. Use `TemporalExtractionAgent.extract()` directly.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Metadata RAG for backfill | `backfill_cohort_years.py` used `test_metadata_rag_subgraph` — wrong architecture, RAG is not needed for metadata parsing | advisory | Already fixed in `LangGraph/scripts/backfill_cohort_years.py` |
| Exact filename match | `find_urls_by_filename()` does exact match — fails for firecrawl-truncated names | blocking | Fix with prefix/fuzzy stem matching before re-index |

<current_state>
Working on Phase 2 infrastructure: fixing file_path coverage (68% docs missing).

Root cause found: `get_url()` in `LangGraph/src/agent/utils.py` fails to match firecrawl-downloaded PDFs because:
1. Firecrawl truncates long filenames
2. Appends 8-char hash suffix (e.g., `-65b48241.pdf`)

Example mismatch:
- Disk: `795_qd_...truong_dh_cntt_va_truong-65b48241.pdf` (truncated + hash)
- Markdown URL: `795_qd_...truong_dh_cntt_va_truong_dh_newcastle.pdf` (full name)

Hash suffix stripping already added but not enough — stems still don't match due to truncation.
</current_state>

<completed_work>

- ✅ Migrated `LangGraph/scripts/backfill_cohort_years.py`: replaced `test_metadata_rag_subgraph` with `TemporalExtractionAgent`
- ✅ Tested backfill: 6/10 success, 4 failed (no file_path)
- ✅ Identified 349/509 docs (68%) missing file_path in `lightrag_doc_status`
- ✅ Root cause: `get_url()` fails → `file_source=None` → no URL saved at index time
- ✅ Rebuilt `pdf_url_lookup.json` (159 → 182 entries via `rebuild_pdf_lookup.py`)
- ✅ Added hash suffix stripping in `utils.py:121`
- ✅ Added fallback URL construction from file path pattern in `utils.py`
- 🔄 Fix incomplete: truncation mismatch still breaks match
</completed_work>

<remaining_work>

- **Task 6 (NEXT):** Fix `find_urls_by_filename()` in `LangGraph/src/agent/utils.py` — implement prefix/fuzzy stem matching:
  - Strip hash suffix from disk filename
  - Check if disk stem is a prefix of URL stem (or vice versa)
  - Threshold: if prefix covers ≥60% of URL stem characters → match
- **Task 7:** Re-index 349 docs that currently have no file_path (once get_url() works)
- **Task 8:** Run full backfill: `python LangGraph/scripts/backfill_cohort_years.py`
</remaining_work>

<decisions_made>

- Use `TemporalExtractionAgent` not metadata RAG subgraph for all extraction
- Target 100% metadata coverage by lowering confidence threshold 0.7 → 0.5
- No OCR re-run needed — existing `.md` files from MinerU sufficient
- Conservative disclaimer strategy: only show "hỏi cố vấn" warning when confidence low
- Universal cohort_scope (`*`) should score 1.0 in reranker (always valid)
</decisions_made>

<blockers>

- Firecrawl filename truncation: disk names are shorter than URL names → exact match fails. Need fuzzy matching.
</blockers>

## Required Reading (in order)

1. `LangGraph/src/agent/utils.py:18-144` — `get_url()` and `find_urls_by_filename()` — what needs fixing
2. `LangGraph/scripts/backfill_cohort_years.py` — migrated script, ready to run after fix
3. `.planning/ROADMAP.md` — Phase 2 context

## Critical Anti-Patterns (do NOT repeat these)

- **METADATA RAG FOR EXTRACTION**: Do NOT use `test_metadata_rag_subgraph` for any extraction task. Always use `TemporalExtractionAgent.extract()` directly.
- **EXACT FILENAME MATCH ON FIRECRAWL FILES**: Firecrawl truncates + hashes filenames. Never assume disk name == URL filename. Always strip hash suffix and use prefix matching.

## Infrastructure State

- LightRAG: running on `localhost:9622`
- PostgreSQL: running on `localhost:5433` (container: `uit_docs_agent-postgres_uit-1`)
- Qdrant: running on `localhost:6333`
- venv: `source .venv/bin/activate` (from project root)
- MinerU OCR: remote at `http://100.102.11.75:8000`

## Files Modified (uncommitted)

- `LangGraph/src/agent/utils.py` — hash strip + fallback URL construction (incomplete, needs fuzzy match)
- `LangGraph/scripts/backfill_cohort_years.py` — migrated to TemporalExtractionAgent
- `rebuild_pdf_lookup.py` — new script to rebuild pdf_url_lookup.json from markdowns

<context>
Deep investigation into why 68% of docs missing file_path. Traced through indexing pipeline:
indexing_graph.py:585 calls get_url(file_path) → None → file_source=None → no URL stored → no citation possible.

Fix strategy: make get_url() smarter about truncated filenames. Once fixed, re-index 349 orphaned docs.
Then run full backfill to get 100% temporal metadata coverage.
</context>

<next_action>
Start with: Fix `find_urls_by_filename()` in `LangGraph/src/agent/utils.py` to use prefix/fuzzy matching.

Specifically:
1. In `get_url()` after stripping hash suffix from `target_name`
2. Modify matching to: `url_stem.startswith(target_stem)` (prefix match)
3. Test: `get_url('firecrawl/data/daa/.../795_qd_...-65b48241.pdf')` should return `https://daa.uit.edu.vn/sites/daa/files/202506/795_qd_..._newcastle.pdf`
4. Then: check how many of 349 orphaned docs now get URLs
5. Then: re-index or backfill those docs
</next_action>
