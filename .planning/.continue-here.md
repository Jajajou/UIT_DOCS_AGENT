---
context: research
phase: 2-temporal-intelligence
task: investigation
status: blocked
last_updated: 2026-05-01T07:17:20Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Qdrant metadata sync regression — Syncing PostgreSQL metadata to Qdrant caused -6.67pp accuracy drop (60% → 53.33%) due to 86% of docs missing cohort_years metadata. Filter logic treats empty cohort_years as explicit mismatch, dropping valid docs.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Qdrant sync without metadata validation | Synced cohort_years=[] to Qdrant for 440/509 docs (86%), causing filter to drop valid results | blocking | Always validate metadata population rate BEFORE syncing to Qdrant. Check PostgreSQL first: `SELECT COUNT(*) WHERE cohort_years IS NULL OR cohort_years = '[]'` |

<current_state>
Investigating Qdrant sync regression after backfilling PostgreSQL temporal metadata. Root cause identified: 86% of docs missing cohort_years. Created backfill script but extraction logic needs fixing before running full backfill.

**Files:**
- `/Users/jajajou1778/UIT_DOCS_AGENT/sync_qdrant.py` — Qdrant sync script (executed, 2241 points updated)
- `/Users/jajajou1778/UIT_DOCS_AGENT/LangGraph/scripts/backfill_cohort_years.py` — Cohort backfill script (created, needs fix)
- `/Users/jajajou1778/UIT_DOCS_AGENT/LangGraph/tests/eval/res_post_qdrant_sync.json` — Post-sync eval results (53.33% accuracy)
</current_state>

<completed_work>

**Session accomplishments:**
1. ✓ Phase 2 research validated (2025-04-14 research still current for 2026)
2. ✓ Qdrant metadata sync executed (2241 points, 509 docs)
3. ✓ Post-sync evaluation run (30 queries, validation split)
4. ✓ Root cause identified: 86% docs missing cohort_years in PostgreSQL
5. ✓ Filter logic analyzed (query_graph.py lines 245-266)
6. ✓ Backfill script created (backfill_cohort_years.py)
7. ✓ Memory saved (project_qdrant_sync_regression.md, project_qdrant_cohort_bug.md)

**Key findings:**
- Accuracy regression: 60% → 53.33% (-6.67pp)
- PostgreSQL: 440/509 docs (86.4%) have empty cohort_years
- Filter drops items with explicit cohort mismatch (line 261-262)
- Qdrant sync wrote cohort_years=[] for all docs
- Filter now interprets [] as explicit empty list, not missing data

</completed_work>

<remaining_work>

**Immediate:**
1. Fix backfill_cohort_years.py extraction result parsing (script marks successful extractions as failed)
2. Run full cohort backfill (440 docs)
3. Re-sync Qdrant with updated cohort metadata
4. Re-run eval to measure improvement

**Alternative quick fix:**
- Update filter logic in query_graph.py to treat cohort_years=[] as neutral (universal access) instead of explicit mismatch

</remaining_work>

<decisions_made>

1. **Chose backfill over filter fix** — Proper solution is to populate missing cohort_years metadata, not work around it in filter logic
2. **Used metadata RAG subgraph** — Leverages existing 6-node extraction workflow with 0.92 confidence
3. **Created new backfill script** — Existing scripts (fix_amends_extraction.py, backfill_temporal_metadata.py) don't handle cohort extraction

</decisions_made>

<blockers>

**Technical:**
- Backfill script result parsing mismatch — test_metadata_rag_subgraph returns different structure than expected
- Script checks `"metadata" not in result` but function returns validated metadata directly

**Context:**
- Context at 75% — Need fresh session for full backfill execution

</blockers>

## Required Reading (in order)

1. `.planning/phases/2-temporal-intelligence/2-RESEARCH.md` — Phase 2 temporal intelligence research (validated 2026-05-01)
2. `/Users/jajajou1778/.claude/projects/-Users-jajajou1778-UIT-DOCS-AGENT/memory/project_qdrant_sync_regression.md` — Regression analysis
3. `/Users/jajajou1778/.claude/projects/-Users-jajajou1778-UIT-DOCS-AGENT/memory/project_qdrant_cohort_bug.md` — Root cause details

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: Syncing metadata to Qdrant without validating population rate → Always check PostgreSQL metadata completeness before Qdrant sync
- **ANTI-PATTERN**: Assuming empty list means "no data" in filter logic → Distinguish between missing metadata (None) and explicit empty list ([])

## Infrastructure State

- **PostgreSQL**: 509 docs indexed, 440 missing cohort_years (86.4%)
- **Qdrant**: 2241 points updated with cohort_years=[] (causing over-filtering)
- **LightRAG**: Running at http://localhost:8020
- **Reranker**: HTTP mode at http://rxg:8080
- **Eval baseline**: 60% accuracy (pre-sync), 53.33% accuracy (post-sync)

<context>

**Mental model:**
The Qdrant sync was supposed to enable COHORT retrieval path and improve accuracy. Instead, it caused regression because we synced incomplete metadata. The filter logic is correct — it's designed to drop docs with explicit cohort mismatch. The bug is that we wrote cohort_years=[] to Qdrant for docs that should have had cohort_years populated.

**Approach:**
1. Backfill missing cohort_years in PostgreSQL using metadata RAG subgraph
2. Re-sync to Qdrant with complete metadata
3. Re-run eval to confirm improvement

**Script status:**
backfill_cohort_years.py runs but marks successful extractions as failed. Test output shows:
```
Metadata validated and formatted: {'document_number': '133/QD-ĐHCNTT', 'valid_from': '2025-02-17', 'cohort_years': ['*'], 'cohort_scope': 'universal', 'amends_documents': [], 'extraction_confidence': 0.84}
[doc-0062c0d5c22c3ba62771538a57190f64] ✗ Extraction failed
```

The metadata is extracted correctly but script logic needs fixing.

</context>

<next_action>

**Start with:**
1. Read test_metadata_rag_subgraph return structure in `/Users/jajajou1778/UIT_DOCS_AGENT/LangGraph/src/agent/graphs/metadata_rag_subgraph.py`
2. Fix backfill_cohort_years.py result parsing (lines 60-65)
3. Test on 5 docs: `cd LangGraph && python scripts/backfill_cohort_years.py --dry-run --limit 5`
4. Run full backfill: `python scripts/backfill_cohort_years.py --limit 440`
5. Re-sync Qdrant: `cd .. && python sync_qdrant.py`
6. Re-run eval: `cd LangGraph && python tests/eval/run_evaluation.py --pairs tests/eval/temporal_test_pairs_100.json --split validation --out tests/eval/res_post_backfill.json`

</next_action>
