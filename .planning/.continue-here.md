---
context: research
phase: none (ad-hoc OCR output quality scan, not a GSD phase/spike)
status: in_progress
last_updated: 2026-08-11T15:58:35.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: The old MinerU `content_list len=0` / garbage-loop bug documented below this section (sessions 1-4, 2026-08-10) is SUPERSEDED. Root cause was confirmed and fixed later the same window — see memory `feedback_mineru_garbage_output_root_cause` (missing `tie_word_embeddings` in config.json + broken `logits_processors` on vLLM V1). That instance (47343687) was destroyed 2026-08-11 after a clean smoke test. Do not re-investigate vLLM/Qwen2VL internals from scratch — start from that fix.
- [ ] CONSTRAINT: This session found the SAME symptom (garbage-token repetition loop) recurring in a *different* batch OCR run (ctsv/daa documents, port 8080). It is NOT YET CONFIRMED whether this batch ran on a server with the fix applied or an older/different unpatched instance. Do not assume it's the same bug re-appearing until config.json on the port-8080 server is checked.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting HTTP 200 as success signal for MinerU | `/file_parse` can return 200 OK even when output is garbage/degenerate | blocking | Always inspect actual extracted content for repeat-loop patterns, not just status code |
| Assuming a repeat-loop regex match = a real bug | grep for repeat-loop pattern on the ctsv/daa batch matched 171/1236 files, but many are false positives (markdown table separator rows `\| --- \|`, base64 chunks, legitimate LaTeX repetition) | advisory | Manually inspect a sample of matches before reporting a bug rate; don't trust raw grep count |

<current_state>
Scanning a batch OCR output (ctsv/daa document set, ~1236 markdown files) for recurrence of the MinerU garbage-token repetition-loop bug (previously root-caused and fixed on a now-destroyed instance, see memory `feedback_mineru_garbage_output_root_cause`).

Manually checked 4 files: 1 has the bug (`523_qd_dhcntt30_8_2021-85fa0d1d.md` — rowspan table cell for SV #24 causes infinite token repetition, swallowing SV #24-32 from the student list table; main legal content in Điều 1-3 is unaffected). Other 3 files clean.

Ran a broad regex grep across the full 1236-file output directory for the repeat-loop pattern: 171 files matched. This count is an unfiltered upper bound — includes false positives (table separators, base64, valid LaTeX repetition). True garbage-loop count is estimated ~130-150 files (~11-12%) but not yet confirmed by manual filtering.

Batch was processed via port 8080, a different server/instance than the one where root cause was fixed and smoke-tested clean (instance 47343687, destroyed). Not yet verified whether port 8080's server has the same `tie_word_embeddings`/`logits_processors` patch applied.
</current_state>

<completed_work>

Completed Tasks:
- Manually inspected 4 files from ctsv/daa OCR batch for garbage-token bug - Done (1 bad, 3 clean)
- Regex grep across full 1236-file output dir for repeat-loop pattern - Done (171 raw matches, unfiltered)
</completed_work>

<remaining_work>

- Filter the 171 regex matches to separate true garbage-loop positives from false positives (table separators, base64, LaTeX) to get an accurate bug count
- Check port-8080 MinerU server's `config.json` for the `tie_word_embeddings`/`logits_processors` patch (see memory `feedback_mineru_garbage_output_root_cause`) — determines whether this is the same bug recurring on an unpatched server, or a new/different issue
- If unpatched: apply the known fix, then rerun the confirmed-garbage files through the patched server
- If already patched and bug still occurs: this is a NEW failure mode (rowspan-table-specific?), needs fresh investigation — do not assume the old fix covers it
</remaining_work>

<decisions_made>

- Stopped at diagnosis, did not start fix/rerun this session, due to context budget (~75%) — avoid starting new work without enough budget to finish safely
</decisions_made>

<blockers>
- Unconfirmed whether port-8080 server has the MinerU fix applied. Blocks deciding between "just rerun files" vs "patch server first, then rerun."
</blockers>

## Required Reading (in order)
1. Memory `feedback_mineru_garbage_output_root_cause` — the known fix (tie_word_embeddings + logits_processors), confirmed working on RTX 3060 before instance teardown
2. Memory `reference_mineru_deploy_skill.md` — use `/mineru-deploy` skill for any redeploy/patch, not manual steps
3. This file's `<current_state>` — where the batch-scan left off

## Infrastructure State
- Batch OCR output directory: ~1236 markdown files (ctsv/daa document set), reachable locally
- Server used for this batch: port 8080 (identity/patch status of underlying instance not yet confirmed)
- Previous fixed+verified instance (47343687) already destroyed — cannot re-check it directly, rely on memory record

<context>
User asked whether more files than the 1 spot-checked might be affected, and whether it's dangerous. Answer given: yes, likely 11-14% of the batch (upper bound, not yet filtered for false positives), and it's dangerous in a "silent data corruption" sense — main legal text stays correct, but appendix tables (student lists, awards) lose rows silently, which could feed wrong data into the RAG index without anyone noticing. Root technical cause: VLM decoding gets no clear stop signal on visually-empty/repetitive rowspan table cells, so it loops emitting the last token until max_tokens is hit — a known decode failure mode for this class of scanned Vietnamese admin documents.
</context>

<next_action>
Resume by checking port-8080 MinerU server's config.json for the known patch (tie_word_embeddings + logits_processors), then manually filter the 171 regex matches into true-positive garbage-loop files vs false positives, to get an accurate affected-file count before deciding on a rerun.
</next_action>
